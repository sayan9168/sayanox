# Stage-5 — Semantic Ownership Shift

Stage-5 is a historical self-hosting milestone. Its numbered shell driver has been retired.

The supported build path is now:

```bash
make stage2
bash selfhost/bootstrap_production.sh
```

The Sayanox source artifacts from this milestone remain available for compiler development, but `selfhost/stage*.sh` scripts are not supported entry points.
