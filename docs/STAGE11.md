# Stage-11 — Control-Flow IR Pipeline

Stage-11 is a historical self-hosting milestone. The numbered shell driver has been retired.

Supported build path:

```bash
make stage2
bash selfhost/bootstrap_production.sh
```

`selfhost/ir_pipeline.sa` remains available for compiler development; numbered stage shell scripts are not supported entry points.
