# Stage-8 — Checked AST Boundary

Stage-8 is a historical self-hosting milestone. Its numbered shell smoke driver has been retired.

Supported bootstrap:

```bash
make stage2
bash selfhost/bootstrap_production.sh
```

`selfhost/checked_ast.sa` remains part of the source pipeline. Numbered `stage*.sh` scripts are no longer supported.
