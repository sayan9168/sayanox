# Stage-6 — Scoped Type Environment

Stage-6 is a historical self-hosting milestone. The numbered Stage-6 shell driver has been retired.

Use the supported bootstrap path instead:

```bash
make stage2
bash selfhost/bootstrap_production.sh
```

`selfhost/typeenv.sa` remains part of the compiler source tree; `stage*.sh` files are not supported entry points.
